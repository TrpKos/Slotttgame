let coins = 0; let level = 1; let coinsPerClick = 1; let upgradeCost = 10; const coinsDisplay = document.getElementById("coins"); const levelDisplay = document.getElementById("level"); const upgradeButton = document.getElementById("upgrade-btn"); const slotMachine = document.getElementById("slot-img"); // Aktualizace zobrazení function updateDisplay() { coinsDisplay.textContent = `Coins: ${coins}`; levelDisplay.textContent = `Level: ${level}`; upgradeButton.textContent = `Upgrade (Cost: ${upgradeCost} coins)`; upgradeButton.disabled = coins < upgradeCost; } // Kliknutí na výherní automat slotMachine.addEventListener("click", () => { coins += coinsPerClick; updateDisplay(); }); // Kliknutí na tlačítko vylepšení upgradeButton.addEventListener("click", () => { if (coins >= upgradeCost) { coins -= upgradeCost; level++; coinsPerClick++; upgradeCost = Math.floor(upgradeCost * 1.5); updateDisplay(); } }); // Inicializace hry updateDisplay();